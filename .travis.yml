language: node_js

node_js:
  - 6

cache:
  directories:
    - node_modules
    - $HOME/.cache

install:
  - npm install

before_script:
  - npm run tsc

script:
  - npm test

notifications:
  email:
    on_success: never
    on_failure: change
